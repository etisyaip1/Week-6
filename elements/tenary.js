

// ?

let inout = true;

let option = undefined; //

if(input){
    option = "yes we can";
}else{
    option = "no we will not";
}

console.log("OPTION:" , option);
//////////////////////////////

const lightColor = "RED";
// EXPRESSION ? IF_TRUE : IF FALSE
const action = lightColor === "GREEN" ? "GO GO GO!!!" : "STOP!!!";
console.log(action);